---
interface Props {
	title: string;
}

const { title } = Astro.props;
---

<!doctype html>
<html lang="es">
	<head>
		<meta charset="UTF-8" />
		<meta name="description" content="Sistema de Citas Médicas" />
		<meta name="viewport" content="width=device-width" />
		<link rel="icon" type="image/svg+xml" href="/favicon.svg" />
		<meta name="generator" content={Astro.generator} />
		<title>{title}</title>
	</head>
	<body class="bg-slate-50 min-h-screen flex flex-col font-sans text-slate-900">
		<header class="bg-white shadow-sm sticky top-0 z-10">
			<nav class="container mx-auto px-4 py-4 flex justify-between items-center">
				<a href="/" class="text-2xl font-bold text-blue-600 flex items-center gap-2">
					🏥 Citas<span class="text-slate-800">Astro</span>
				</a>
				<div class="space-x-4">
					<a href="/" class="text-slate-600 hover:text-blue-600 font-medium transition-colors">Doctores</a>
					<a href="#" class="text-slate-600 hover:text-blue-600 font-medium transition-colors">Mis Citas</a>
				</div>
			</nav>
		</header>

		<main class="flex-grow container mx-auto px-4 py-8">
			<slot />
		</main>

		<footer class="bg-slate-900 text-slate-400 py-8 mt-auto">
			<div class="container mx-auto px-4 text-center">
				<p>&copy; {new Date().getFullYear()} Sistema de Citas Astro. Demo para Práctica de Agentes.</p>
			</div>
		</footer>
	</body>
</html>
